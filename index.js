import {
    readFile,
    validateNum,
    copyText,
    setNumber,
    getDate,
    getTimer,
    strToZeroWidth,
    zeroWidthToStr,
    formatterNumber,
    secondConversion,
    scrollToSelected,
    validatePhoneNum
} from './src/tool.js'
import witDB from './src/indexDB'
import incrementCounter from "./src/incrementCounter";
export {
    readFile,
    validateNum,
    copyText,
    setNumber,
    getDate,
    getTimer,
    strToZeroWidth,
    zeroWidthToStr,
    formatterNumber,
    validatePhoneNum,
    secondConversion,
    scrollToSelected,
    incrementCounter,
    witDB
}